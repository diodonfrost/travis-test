mariadb_packages:
  - '{{ "MariaDB-server"
        if (install_from == "mariadb_repository")
        else "mariadb-server" }}'
  - MySQL-python

mariadb_service: '{{ "mariadb"
                     if (ansible_distribution_major_version == "7" and mariadb_version int >= 10.1 )
                     else "mysql" }}'

mariadb_config_file: /etc/my.cnf.d/server.cnf
mariadb_config_file_owner: root
mariadb_config_file_group: root
